<script>
  Vue.component('v-select', VueSelect.VueSelect)

  let app = new Vue({
    el: '.receipts',
    mixins: [m_created, m_index],
    data: <%= fill_vue_data(nil, { 
      sortKey: 'sortdate',
      controller: 'receipts',
      reverse: true,
      currentIndex: -1,
      currentMonth: -1,
      confirmModal: false,
      groupHeaders: [],
      filter: [],
      grouped: [],
      filteredData: [],
      list_values: 'account receipts_type',
      lists: 'accounts receipts_types users mainList:raw@json_receipts',
      columns: [['date', 'Дата'], ['account', 'Счет'], ['amount', 'Сумма'],
                 ['receipts_type', 'Категория'],
                ['comment', 'Комментарий']]
      
    }) %>,

})
</script>